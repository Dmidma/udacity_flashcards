import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    container: {
        borderWidth: 2
    },
    fonty: {
        fontSize: 20
    },
    pretty: {
        color: 'red',
        fontWeight: 'bold'
    }
})

export default styles
